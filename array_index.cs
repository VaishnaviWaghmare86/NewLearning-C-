using System;

class Program {
    static void Main() {
        int[] nums = {10, 20, 30};
        int index = Array.IndexOf(nums, 20);
        Console.WriteLine("Index of 20: " + index);
    }
}
